[project]
name = "hashdog"
version = "1.0.2"
description = "Simple hash identifier"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "rich>=15.0.0",
]

[project.scripts]
hashdog = "main:main"
